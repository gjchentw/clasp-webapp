# clasp-webapp

```bash

npm install -g @google/clasp

clasp login 

# copy forwared codespace url to replace hos tpart of oauth uri http://localhost:port/...

# create a gas project, get project id

clasp clone 'project_id'

npm i @types/google-apps-script

```